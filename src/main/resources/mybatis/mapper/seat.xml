<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.goodee.gdlibrary.mapper.SeatMapper">
	
	<select id="selectSeatList" parameterType="List" resultType="SeatDTO">
		SELECT SEAT_NO, SEAT_STATUS, SEAT_USING_STATE
		  FROM SEAT
		 ORDER BY SEAT_NO DESC
	</select>

	<select id="selectSeatByNo" parameterType="Long" resultType="SeatDTO">
		SELECT SEAT_NO, SEAT_STATUS, SEAT_USING_STATE
		  FROM SEAT
		 WHERE SEAT_NO = #{seatNo}
	</select>
	
	<update id="updateUpSeatUsingState" parameterType="Long">
		UPDATE SEAT
		   SET SEAT_USING_STATE = 2
		 WHERE SEAT_NO = #{seatNo}
	</update>

</mapper>